import React from "react"

import {Input,Button} from "antd"

class MessageOfRet extends React.Component{

    constructor(props){
        super()
    }

    handle

    render(){
        return(<div>
            <Input onChange={} onPressEnter={}/>
            <Button type={"primary"} size={"small"}  onClick={} />
        </div>)
    }
}