import React from "react"

import {Popover} from "antd"

export default class BookIntroModal extends React.Component{

    state={

    };

    render(){

        return(
            <Popover content={} title={"popover"} trigger={"hover"}>

            </Popover>
        )

    }

}